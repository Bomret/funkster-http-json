{
  "name": "funkster-http-json",
  "version": "0.7.0",
  "description": "Funkster is a compositional server library. This package provides types and combinators to parse json bodies from requests and respond with json to the client.",
  "license": "Unlicense",
  "repository": "git@github.com:Bomret/funkster-http-json.git",
  "author": "Stefan Reichel <dev@bomret.com>",
  "keywords": [
    "server",
    "node",
    "funkster",
    "http",
    "web",
    "json",
    "body"
  ],
  "files": [
    "index.js",
    "lib",
    "package.json",
    "README.md",
    "CHANGELOG.md"
  ],
  "main": "index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "build": "npm run clean && npm run lint && echo Using TypeScript && tsc --version && tsc --pretty",
    "test": "npm run build && mocha --compilers ts:ts-node/register --recursive test/**/*-spec.ts",
    "watch": "npm run build -- --watch",
    "watch:test": "npm run test -- --watch"
  },
  "dependencies": {
    "funkster-http-headers-accept": "^0.5.0",
    "funkster-http-headers-content": "^0.6.0"
  },
  "devDependencies": {
    "@types/accepts": "^1.3.0",
    "@types/chai": "3.4.34",
    "@types/content-disposition": "^0.5.0",
    "@types/content-type": "^1.0.33",
    "@types/mocha": "2.2.33",
    "@types/node": "6.0.51",
    "@types/supertest": "^2.0.0",
    "chai": "^3.5.0",
    "mocha": "^3.0.1",
    "rimraf": "^2.5.4",
    "supertest": "^2.0.1",
    "ts-node": "^1.2.2",
    "tslint": "4.0.2",
    "typescript": "^2.1.4"
  },
  "engines": {
    "node": ">=4.6.1"
  }
}
